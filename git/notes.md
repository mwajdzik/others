

```
git init
 - or
git clone https://github.com/libgit2/libgit2

git add *.c
git add LICENSE
git commit -m 'initial project version'

git status
git status -s

git add README					- track and stage a file, after any modification issue to stage again

```
